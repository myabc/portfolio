<h1>Editing user</h1>

<%= error_messages_for :user %>

<% form_for(@user) do |f| %>
    <%= render :partial => 'form', :locals => { :form => f } %>
    <%= f.submit "Update" %>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>

